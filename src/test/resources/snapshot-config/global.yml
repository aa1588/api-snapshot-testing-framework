# Global Snapshot Testing Configuration
# =====================================
# These masking rules apply to ALL endpoints.
# For this demo, we keep it minimal to show endpoint-specific masking.

masking:
  paths:
    # Only very common envelope fields
    - $.responseId
    - $.correlationId
